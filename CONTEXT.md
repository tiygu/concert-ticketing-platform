# 领域术语表

大型演唱会票务预订与VIP服务平台。

## 角色

| 术语 | 英文 | 说明 |
|------|------|------|
| 管理员 | Admin | role=ADMIN，拥有后台全部管理权限（用户管理、演出管理、订单审核、公告发布、数据统计、VIP套餐配置） |
| 普通用户 | User | role=USER + vip_level=0，可浏览演出、选座购票、管理个人订单 |
| VIP用户 | VIP User | role=USER + vip_level≥1，继承普通用户全部功能，额外享有VIP权益预约 |

## 核心实体

| 术语 | 表名 | 说明 |
|------|------|------|
| 演出 / 场次 | shows | 演唱会排期信息（演出名称、时间、场馆、票价档位、座位总数） |
| 选票 / 票品 | tickets | ticket_type 区分 REGULAR（普通票）或 VIP（VIP票），按座位粒度管理库存 |
| 订单 | orders | 用户购票交易记录，pay_status 跟踪生命周期 |
| VIP权益套餐 | vip_packages | 管理员上架的 VIP 专属服务套餐（如后台探班、签名周边、优先入场等） |
| VIP预约 | vip_bookings | VIP用户对套餐的预约申请，需管理员审核 |
| 公告 | notices | 管理员发布的演出排期、活动通知、规则变更 |
| 评价 | reviews | 用户对已完成订单的演出体验评价（1-5 评分） |

## 订单生命周期

```
PENDING（待支付）
  ├─ 用户支付 → PAID（已支付）
  │               └─ 用户确认 → COMPLETED（已完成）
  ├─ 用户取消 → CANCELLED（已取消）
  ├─ 超时未付 → CANCELLED（已取消）
  └─ 管理员驳回 → REJECTED（已驳回）
```

## 技术术语

| 术语 | 说明 |
|------|------|
| 预锁座 | 用户选座后先锁定座位（行级锁），生成待支付订单，超时释放 |
| 积分兑换 | VIP用户使用 points 积分抵扣 VIP 票的部分价格 |
| 行级锁 | InnoDB SELECT ... FOR UPDATE，保证座位扣减与订单生成的原子性 |
